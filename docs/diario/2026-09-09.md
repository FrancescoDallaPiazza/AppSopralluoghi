# 9 settembre 2026

Due sessioni in parallelo, due repo: qui **AppSopralluoghi** (fase 0, tenere a
galla la barca), di là **AppFormazione** (fase 1, chiudere le fonti). Questa
pagina traccia solo quello che è successo qui.

## Fatto

| commit | cosa |
|---|---|
| `af0aefb` | **D1** cache voci + **D4** migrazione `063_tecnico_cognome.sql`. Commit fatto e pushato dall'esterno della sessione, con messaggio `09.09`: il contenuto è giusto, la traccia del perché no — sta qui e nel TODO. |
| `bcc3a31` | Completa D1: nel commento resta scritto il nesso con le RLS, cioè che oggi cachare zero voci è sicuro *solo* perché le policy sono tutte `using (true)`. |
| `33e5838` | **D3** quarantena della coda offline. Classificazione dell'errore, `db.quarantena` (Dexie v7), pausa di 30s dopo un errore ritentabile, foto non più perse in silenzio. |

| `dfa1ff6`, `6bdf8e1`, `7702842` | La traccia: TODO allineato al vero, `docs/diario/`, e il puntatore al programma dei lavori con il testo canonico. |
| `0d0c8a0` | **I sette buchi dell'import.** Vocabolario (colonne "LEGALE", `N. DIPENDENTI`, `Data di Licenziamento`, `Area di Lavoro`), filtro sulla colonna `ATTIVA`, guardia sulla P.IVA, e il ripiego cognome+nome per chi non ha il codice fiscale. |

Fuori dal repo, nella conversazione:

- **Dossier di verifica dello scorporo** — tre revisioni avversariali in sola
  lettura sull'analisi 0-bis. Materiale integrale in
  `docs/_recupero-analisi-2026-09-09.md`, reso condivisibile come artifact.
- **Cronoprogramma del repo unico** — nato dalla fusione di due programmi
  scritti in parallelo nelle due sessioni. È il documento unico: il programma
  redatto qui è stato ritirato e rimanda a quello.

## Aperto qui, in ordine

1. **Ricreare i clienti** — l'import ora legge quello che deve, ma le 618
   aziende non sono ancora rientrate. Serve `ElencoSedi (5).xlsx`, che **non è
   più in `~/Downloads`**: c'è solo `ExportExcelDipendenti.xlsx`.
2. **La schermata della quarantena** — il dato c'è (`contaQuarantena`),
   l'interfaccia no.
3. **D2, il report che non conosce i componenti** — richiede un deploy di Edge
   Function, non solo un push.

## Cosa è stato provato, e cosa no

Le riparazioni **lato persone** sono verificate sull'export vero
(`ExportExcelDipendenti.xlsx`, 3.418 righe, intestazione alla riga 3):
`Data di Licenziamento` e `Area di Lavoro` ora agganciano; 95 P.IVA su 3.416
sono scartate come chiave (`00000000000`, `0418754028` a dieci cifre,
`IT01476520`); delle 235 righe senza codice fiscale, **227 hanno un nome
univoco** e il ripiego le aggancia, 6 restano `riga:N` perché omonime.

Le riparazioni **lato clienti** (colonne `LEGALE`, `ATTIVA`, `N. DIPENDENTI`)
**non sono state riprovate sui dati**: i nomi di colonna vengono dal riscontro
del 26 agosto. Da rifare quando `ElencoSedi` torna disponibile.

## Da sapere per domani

- **`bcc3a31` e `33e5838` non sono pushati.** Il push è quello che innesca il
  deploy Vercel: va fatto quando si vuole rilasciare, non per abitudine.
- **Il raccordo ATECO deve arrivare a monte prima** che si cominci a riempire
  l'ATECO mancante dalle visure camerali. L'import dei due Excel non è a
  rischio (`ElencoSedi` porta codici in formato ATECO 2007, e la tabella di
  `risolviAteco` è 2007: combaciano); una visura di oggi porta invece un codice
  **2025**, ed è lì che i 62 codici che cambiano classe diventano un livello di
  rischio sbagliato scritto in anagrafica, senza segnale.
- **Attenzione ai commit anonimi.** Oggi `af0aefb` ha inglobato due riparazioni
  distinte sotto il messaggio `09.09`. Il contenuto non si perde, la ragione sì.

## L'altra corsia

*Letto dal loro repo, non riferito: `git -C ../AppFormazione log --since=...`.*
Oggi lì: `479cef4` (le quattro letture dell'Allegato IV erano due, e vanno
d'accordo), `7acfd69` (lo stato diceva che non c'era codice, e il codice c'era da
due giorni), `a142e2a` (il piano esce da claude.ai ed entra nel repo, una volta
sola — `docs/PROGRAMMA.md`).

Fase 1, in `AppFormazione`. Chiuso oggi: le letture indipendenti dell'Allegato IV
sono **due** e concordano su tutte e 85 le divisioni condivise — il repo unico
eredita una regola sola; e l'asse dell'annata è misurato, **62 codici** su 2.166
cambiano classe. Aperto di là: l'Interpello MLPS 1/2025 con la trascrizione, le
divisioni 30/86/87, il «si prende il più alto», e il raccordo ISTAT da restituire
alla libreria normativa.

**Cosa aspettiamo da loro:** il raccordo a monte, in `formazione-81-utils-src`.
Finché non c'è, riempire l'ATECO mancante dalle visure camerali scriverebbe
livelli di rischio sbagliati senza segnale. È l'unico punto in cui questa corsia
aspetta l'altra.

**Cosa dobbiamo noi:** niente.

**Nota sul piano.** `AppFormazione/docs/PROGRAMMA.md` è stato scritto alle 11:30 e
segna ancora come aperti i sette buchi dell'import, chiusi qui un'ora dopo con
`0d0c8a0`. Non è un errore loro: è che le caselle stanno in un repo e il lavoro
nell'altro. Da qui `docs/STATO.md`, che tiene lo stato dove si lavora.
