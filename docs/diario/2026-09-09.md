# 9 settembre 2026

Due sessioni in parallelo, due repo: qui **AppSopralluoghi** (fase 0, tenere a
galla la barca), di là **AppFormazione** (fase 1, chiudere le fonti). Questa
pagina traccia solo quello che è successo qui.

## Fatto

| commit | cosa |
|---|---|
| `af0aefb` | **D1** cache voci + **D4** migrazione `063_tecnico_cognome.sql`. Commit fatto e pushato dall'esterno della sessione, con messaggio `09.09`: il contenuto è giusto, la traccia del perché no — sta qui e nel TODO. |
| `bcc3a31` | Completa D1: nel commento resta scritto il nesso con le RLS, cioè che oggi cachare zero voci è sicuro *solo* perché le policy sono tutte `using (true)`. |
| `33e5838` | **D3** quarantena della coda offline. Classificazione dell'errore, `db.quarantena` (Dexie v7), pausa di 30s dopo un errore ritentabile, foto non più perse in silenzio. |

Fuori dal repo, nella conversazione:

- **Dossier di verifica dello scorporo** — tre revisioni avversariali in sola
  lettura sull'analisi 0-bis. Materiale integrale in
  `docs/_recupero-analisi-2026-09-09.md`, reso condivisibile come artifact.
- **Cronoprogramma del repo unico** — nato dalla fusione di due programmi
  scritti in parallelo nelle due sessioni. È il documento unico: il programma
  redatto qui è stato ritirato e rimanda a quello.

## Aperto qui, in ordine

1. **I sette buchi dell'import anagrafiche** — è il pezzo che rimette dentro le
   618 aziende attive. Il database è vuoto dal 5 agosto.
2. **La schermata della quarantena** — il dato c'è (`contaQuarantena`),
   l'interfaccia no.
3. **D2, il report che non conosce i componenti** — richiede un deploy di Edge
   Function, non solo un push.

## Da sapere per domani

- **`bcc3a31` e `33e5838` non sono pushati.** Il push è quello che innesca il
  deploy Vercel: va fatto quando si vuole rilasciare, non per abitudine.
- **Il raccordo ATECO deve arrivare a monte prima** che si cominci a riempire
  l'ATECO mancante dalle visure camerali. L'import dei due Excel non è a
  rischio (`ElencoSedi` porta codici in formato ATECO 2007, e la tabella di
  `risolviAteco` è 2007: combaciano); una visura di oggi porta invece un codice
  **2025**, ed è lì che i 62 codici che cambiano classe diventano un livello di
  rischio sbagliato scritto in anagrafica, senza segnale.
- **Attenzione ai commit anonimi.** Oggi `af0aefb` ha inglobato due riparazioni
  distinte sotto il messaggio `09.09`. Il contenuto non si perde, la ragione sì.
